package com.timesup.app.focus;

import com.getcapacitor.Plugin;
import com.getcapacitor.annotation.CapacitorPlugin;

@CapacitorPlugin(name = "NativeFocus")
public class NativeFocusPlugin extends Plugin {
    // Minimal Java stub to ensure Java compilation succeeds in CI.
    // The real implementation is provided in Kotlin (NativeFocusPlugin.kt).
}
